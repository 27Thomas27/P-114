/*functions preload, setup and draw*/ 
/*function preload*/
function preload() {
    
}

/*function setup */
function setup() {
    canvas = createCanvas(300,300);
    canvas.center();
}

/*function draw */
function draw() {

}

/*functiuon for taking snapshot */
/*save function and give the name inside it so that it can save with that particular name. */
function take_snapshot(){
    save('myFilterImage.png');
}